When clicking the "browser individual dataset results", when only one dataset is available, we should implement get parameters in the significant-rows page that directly jump to the assay type/disease combination that was selected in the most significant genes page.


On the most significant genes page, we want GET parameters covering the full "state space" of the selected ranking method, assay type, disease, included and excluded genes.