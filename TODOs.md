Introduce input box next to "gene" header in most significant table, so users can search for gene names (or gene name prefixes). However, the ranks should stay the same as for the full list even when filtering for gene names

Where are we getting our list of transcription factors from? What identifies a gene as a TF?

Add "include all"/"include none" buttons to the "show union of", similar to "excluding"

Remove the methods description from the main page; simply put a link next to the ranking method dropdown to the full documentation

Please re-check the assignment of all datasets to diseases, and assay types, for the purposes of meta-analysis. I assume they're correct, but let's double check.

Please also double check the meta analysis code for correctness.



We recently added some "explainer" where p-values come from for a particular gene in the meta-analysis, but I'm not seeing that at all tbh. Where is it? When I click "gene info" in the most significant genes table, there's no info on the p-values and where they're coming from


The "Significant rows by dataset" section might be confusing as it's not linked to the assay type/disease/gene selection above? I believe we should move this to a separate page



change the "only 1 dataset" wording in the most significant page to accommodate the separate page



In assay types / disease selection, list all used datasets for the respective meta analysis and link to them in the "all datasets" page

In the "significance summary" dropdown, remove the combined p-values section --- let's just include the per-dataset breakdown