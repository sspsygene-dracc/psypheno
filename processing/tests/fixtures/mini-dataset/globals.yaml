fieldLabels:
  pvalue: "Nominal p-value"
  padj: "BH-adjusted p-value"
  logfc: "Log fold change"

assayTypes:
  perturbation: "Perturbation Screen"
  expression: "Gene Expression (RNA-seq)"

conditionTypes:
  asd: "Autism"

organismTypes:
  mouse: "Mouse"
  human: "Human"

# Modality taxonomy for the collated overview matrix (#211/#222). One modality
# so the materialization has something to write in the integration test.
modalities:
  - key: perturb_seq
    label: "Perturb-seq"
    assayTypes:
      - perturbation
